interface MonitorPageOptions {
  readonly environment: 'production' | 'test';
}

export function monitorPage({ environment }: MonitorPageOptions): Response {
  const isTest = environment === 'test';
  const environmentLabel = isTest ? ' | AMBIENTE DE TESTE' : '';
  const pageTitle = isTest
    ? 'GTRZ System | Central de sincronizacao - Teste'
    : 'GTRZ System | Central de sincronizacao';
  return new Response(
    `<!doctype html>
<html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${pageTitle}</title><style>
:root{color-scheme:dark;--bg:#0c0c0f;--surface:#17171c;--surface-2:#202027;--line:#34343d;--text:#f5f5f7;--muted:#aaaab7;--red:#ef2344;--green:#3cda88;--blue:#72aaff;--amber:#e8b436}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px Inter,Segoe UI,Arial,sans-serif}main{max-width:1320px;margin:auto;padding:28px 24px 56px}.top{display:flex;align-items:end;justify-content:space-between;gap:18px;padding-bottom:22px;border-bottom:1px solid var(--line)}.eyebrow{margin:0;color:var(--red);font-size:11px;font-weight:800;letter-spacing:.1em;text-transform:uppercase}h1{margin:5px 0;font-size:26px;line-height:1.15}p{margin:0;color:var(--muted)}.access{display:flex;gap:8px}.access input{width:300px;max-width:54vw;height:40px;border:1px solid var(--line);border-radius:5px;background:var(--surface);padding:0 11px;color:var(--text)}button{height:40px;border:0;border-radius:5px;background:var(--red);padding:0 15px;color:#fff;font-weight:800;cursor:pointer}.notice{margin:18px 0;padding:11px 13px;border-left:3px solid var(--amber);background:#2e2510;color:#f8d984}.notice.ok{border-color:var(--green);background:#102b1e;color:#b3f1ce}.hidden{display:none}.metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin:18px 0}.metric,.panel{border:1px solid var(--line);background:var(--surface)}.metric{min-height:96px;padding:14px}.metric span,.metric small{display:block;color:var(--muted);font-size:11px}.metric strong{display:block;margin:7px 0 5px;font-size:22px}.flow{display:grid;grid-template-columns:minmax(0,1fr) 66px minmax(0,1fr) 66px minmax(0,1fr);align-items:center;gap:8px;margin-bottom:18px}.node{min-height:78px;display:grid;place-content:center;text-align:center;border:1px solid var(--line);background:var(--surface-2)}.node b,.node span{display:block}.node span{margin-top:4px;color:var(--muted);font-size:11px}.node.central{border-color:#7b2034}.arrow{height:1px;background:var(--line);position:relative}.arrow:after{content:'>';position:absolute;right:0;top:-9px;color:var(--red)}.grid{display:grid;grid-template-columns:minmax(280px,.8fr) minmax(0,1.5fr);gap:14px}.panel{min-width:0;padding:16px}.panel h2,.r2-panel h2{margin:0 0 3px;font-size:15px}.panel>p,.r2-panel>p{font-size:12px;margin-bottom:12px}.list{max-height:560px;overflow:auto}.entry{display:grid;grid-template-columns:9px minmax(0,1fr) auto;gap:9px;align-items:start;padding:11px 0;border-bottom:1px solid var(--line)}.entry:last-child{border:0}.dot{width:8px;height:8px;border-radius:50%;margin-top:5px;background:var(--green)}.dot.up{background:var(--red)}.dot.down{background:var(--blue)}.entry b,.entry small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.entry b{font-size:12px}.entry small{margin-top:3px;color:var(--muted);font-size:11px}.entry time{color:var(--muted);font-size:11px;white-space:nowrap}.command-list{max-height:640px}.command{padding:14px 0;border-bottom:1px solid var(--line)}.command:last-child{border:0}.command-top{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.command-title{display:flex;gap:9px;min-width:0}.command-title b{display:block;font-size:13px}.command-title p{margin-top:3px;font-size:12px;line-height:1.35}.command time{color:var(--muted);font-size:11px;white-space:nowrap}.command-summary{margin:10px 0 0 17px;color:#e1e1e8;font-size:12px;line-height:1.4}.chips{display:flex;flex-wrap:wrap;gap:6px;margin:10px 0 0 17px}.chip{border:1px solid #3a3a46;border-radius:4px;background:#1d1d24;padding:5px 7px;color:#f0f0f3;font-size:11px}.chip strong{color:var(--green);margin-left:4px}.technical{margin:10px 0 0 17px;border:1px solid #30303a;background:#111116}.technical summary{padding:8px 9px;color:var(--muted);font-size:11px;cursor:pointer;user-select:none}.technical pre{overflow:auto;max-height:260px;margin:0;padding:10px;border-top:1px solid #30303a;color:#c5c5cf;font:11px ui-monospace,Consolas,monospace;white-space:pre-wrap;word-break:break-word}.compact{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px;margin-top:14px}.r2-panel{margin-top:14px;border:1px solid var(--line);background:var(--surface);padding:16px}.device-action{height:30px;padding:0 9px;font-size:11px}.empty{padding:30px 0;text-align:center;color:var(--muted)}@media(max-width:980px){.metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.grid,.compact{grid-template-columns:1fr}.top{align-items:flex-start;flex-direction:column}}@media(max-width:600px){main{padding:18px 13px 42px}.top{gap:14px;padding-bottom:16px}h1{font-size:22px}.access{width:100%}.access input{flex:1;min-width:0;max-width:none}.access button{padding:0 12px}.metrics{grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.metric{min-height:82px;padding:11px}.metric strong{font-size:19px}.flow{grid-template-columns:1fr;margin-bottom:14px}.arrow{width:1px;height:22px;margin:auto}.arrow:after{right:-4px;top:7px;transform:rotate(90deg)}.panel,.r2-panel{padding:13px}.command{padding:13px 0}.command-summary,.chips,.technical{margin-left:0}.command-top{gap:8px}.command-title b{font-size:12px}.command-title p{font-size:11px}.command time{font-size:10px}.list,.command-list{max-height:none}.entry{padding:10px 0}.compact{gap:12px;margin-top:12px}}
</style></head><body><main>
<header class="top"><div><p class="eyebrow">GTRZ System | Cloudflare${environmentLabel}</p><h1>Central de sincronizacao</h1><p>${isTest ? 'Dados isolados para testes. Nada daqui alcança a operação oficial.' : 'Eventos, dispositivos e auditoria em tempo real.'}</p></div><form class="access" id="access"><input id="key" autocomplete="off" placeholder="Chave de pareamento" required type="password"><button>Conectar</button></form></header><p class="notice" id="notice">Informe a chave de pareamento para consultar a central protegida.</p>
<section class="hidden" id="dashboard"><div class="metrics"><article class="metric"><span>Computadores ativos</span><strong id="devices">-</strong><small>Presenca nos ultimos 45 s</small></article><article class="metric"><span>Latencia media</span><strong id="latency">-</strong><small>Medida pelos aplicativos</small></article><article class="metric"><span>Comandos aceitos</span><strong id="accepted">-</strong><small>Diario central atomico</small></article><article class="metric"><span>Repeticoes tratadas</span><strong id="replayed">-</strong><small>Idempotencia preservada</small></article><article class="metric"><span>Entregas observadas</span><strong id="transport">-</strong><small>Push e pull registrados</small></article></div><section class="flow"><div class="node"><b>SQLite local</b><span>Fila e copia autonoma</span></div><div class="arrow"></div><div class="node central"><b>Diario central</b><span>Cloudflare Durable Object</span></div><div class="arrow"></div><div class="node"><b>Outros dispositivos</b><span>WebSocket em tempo real</span></div></section><div class="grid"><section class="panel"><h2>Maquinas conectadas</h2><p>Presenca, origem e latencia.</p><div class="list" id="device-list"></div></section><section class="panel"><h2>Comandos confirmados</h2><p>O que aconteceu, quem enviou e o efeito no estoque.</p><div class="list command-list" id="command-list"></div></section></div><div class="compact"><section class="panel"><h2>Trilhas de transporte</h2><p>Subida e entrega de cada evento.</p><div class="list" id="transport-list"></div></section><section class="panel"><h2>Idempotencia</h2><p>Repetir o mesmo comando nao duplica alteracoes.</p><div class="list" id="idempotency-list"></div></section><section class="panel"><h2>Conflitos reportados</h2><p>Itens que aguardam revisao.</p><div class="list" id="conflict-list"></div></section></div><section class="r2-panel"><h2>Celulares de caixa</h2><p>Sessoes persistentes. Bloquear aqui encerra o acesso do aparelho.</p><div class="list" id="cashier-devices"></div></section><section class="r2-panel"><h2>Arquivo R2</h2><p>Copias imutaveis do diario e snapshots; nao e o banco operacional.</p><div class="list" id="r2-list"></div></section></section></main>
<script>
let key='',timer=null;const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),when=v=>new Intl.DateTimeFormat('pt-BR',{hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(Number(v)),fullTime=v=>new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'medium'}).format(Number(v)),money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0)/100),empty=t=>'<p class="empty">'+esc(t)+'</p>',json=v=>esc(JSON.stringify(v??{},null,2)),short=v=>String(v??'-').slice(0,10).toUpperCase(),entry=(dot,title,meta,stamp)=>'<article class="entry"><i class="dot '+dot+'"></i><div><b title="'+esc(title)+'">'+esc(title)+'</b><small title="'+esc(meta)+'">'+esc(meta)+'</small></div><time>'+when(stamp)+'</time></article>';
function deviceLabel(id,map){return map[id]||('Dispositivo '+short(id))}function actionLabel(action){const x={'operations.order-paid':'Venda confirmada','operations.order-cancelled':'Venda estornada','inventory.stock-moved':'Estoque movimentado','inventory.product-created':'Produto cadastrado','inventory.product-updated':'Produto atualizado','cashier.sale-rejected':'Venda corrigida','event.created':'Evento criado','expense.created':'Despesa registrada','expense.payment-recorded':'Pagamento de despesa','capital.contribution-created':'Aporte registrado','capital.contribution-updated':'Estoque remanescente atualizado','capital.reimbursed':'Reembolso de aporte','cash.opened':'Caixa aberto','cash.closed':'Caixa fechado'};return x[action]||String(action||'Evento confirmado').replace(/[.-]/g,' ')}function commandView(c,labels){const p=c.payload||{},d=p.details||{},action=p.action||c.action,origin=deviceLabel(c.deviceId||p.deviceId,labels);let summary='Registro confirmado pela central.',chips=[];if(action==='operations.order-paid'){const items=Array.isArray(d.items)?d.items:[],point=d.order&&d.order.servicePointLabel?d.order.servicePointLabel:origin;summary='Venda recebida de '+point+'. Total '+money(d.totalCents)+'.';chips=items.map(i=>'<span class="chip">'+esc(i.itemName||'Item')+' <strong>x'+esc(i.quantity||0)+'</strong></span>')}else if(action==='inventory.stock-moved'){const delta=Number(d.delta||0),sign=delta>0?'+':'',product=d.productName||d.productLabel||('produto '+short(d.productId));summary=origin+' movimentou '+sign+delta+' unidade'+(Math.abs(delta)===1?'':'s')+' de '+product+'.';chips=['<span class="chip">Saldo: '+esc(d.beforeQuantity??'?')+' para <strong>'+esc(d.afterQuantity??'?')+'</strong></span>']}else if(action==='expense.payment-recorded'){summary=origin+' pagou '+(d.description||'uma despesa')+' em '+money(d.amountCents)+'.';chips=['<span class="chip">'+esc(d.method||'meio nao informado')+'</span>']}else if(action==='capital.contribution-created'){summary=origin+' registrou aporte de '+(d.contributorName||'responsavel')+' em '+money(d.amountCents)+'.'}else if(action==='capital.reimbursed'){summary=origin+' registrou reembolso prioritario de '+money(d.amountCents)+'.'}else if(action==='operations.order-cancelled'){summary=origin+' estornou uma venda e devolveu o estoque.';chips=['<span class="chip">Devolucao '+money(d.totalCents)+'</span>']}else if(action==='cashier.sale-rejected'){summary='A central corrigiu uma venda que nao foi aplicada na copia local.'}else if(d.name){summary=origin+' registrou '+d.name+'.'}else if(d.description){summary=origin+' registrou: '+d.description+'.'}else{summary=origin+' enviou '+actionLabel(action).toLowerCase()+'.'}return '<article class="command"><div class="command-top"><div class="command-title"><i class="dot up"></i><div><b>'+esc(actionLabel(action))+'</b><p>'+esc(origin)+' | evento '+esc(short(c.eventId))+'</p></div></div><time>'+when(c.createdAt)+'</time></div><p class="command-summary">'+esc(summary)+'</p>'+(chips.length?'<div class="chips">'+chips.join('')+'</div>':'')+'<details class="technical"><summary>Ver dados tecnicos e codigos</summary><pre>'+json({commandId:c.commandId,auditId:c.auditId,eventId:c.eventId,deviceId:c.deviceId,payload:c.payload})+'</pre></details></article>'}
function render(data){const devices=data.activeDevices||[],commands=data.recentCommands||[],transport=data.recentTransport||[],conflicts=data.recentConflicts||[],idem=data.idempotency||{},labels=Object.fromEntries(devices.map(d=>[d.id,d.label])),average=devices.length?Math.round(devices.reduce((n,d)=>n+Number(d.latencyMs||0),0)/devices.length):null;$('devices').textContent=devices.length;$('latency').textContent=average===null?'-':average+' ms';$('accepted').textContent=idem.acceptedCommands??0;$('replayed').textContent=idem.replayedAttempts??0;$('transport').textContent=transport.length;$('device-list').innerHTML=devices.length?devices.map(d=>entry('',d.label,'ID '+short(d.id)+' | '+d.latencyMs+' ms',d.lastSeenAt)).join(''):empty('Nenhuma maquina ativa.');$('command-list').innerHTML=commands.length?commands.map(c=>commandView(c,labels)).join(''):empty('Nenhum comando aceito ainda.');$('transport-list').innerHTML=transport.length?transport.map(t=>entry(t.direction==='up'?'up':'down',t.direction==='up'?'Subiu para a central':'Entregue ao dispositivo',deviceLabel(t.deviceId,labels)+' | '+t.transport+' | '+actionLabel(t.action),t.createdAt)).join(''):empty('Nenhum envio ou recebimento registrado.');$('idempotency-list').innerHTML=entry('','Comandos aceitos',String(idem.acceptedCommands??0)+' alteracoes distintas',data.checkedAt)+entry('','Tentativas de diario',String(idem.journalAttempts??0)+' envios recebidos',data.checkedAt)+entry('','Repeticoes tratadas',String(idem.replayedAttempts??0)+' sem duplicar dados',data.checkedAt);$('conflict-list').innerHTML=conflicts.length?conflicts.map(c=>entry('up',actionLabel(c.action),deviceLabel(c.deviceId,labels)+' | '+c.reason,c.createdAt)).join(''):empty('Nenhum conflito reportado.')}
async function loadCashiers(){const list=$('cashier-devices');try{const r=await fetch('/v1/monitor/cashiers',{headers:{'X-GTRZ-Key':key}});if(!r.ok)throw new Error('Lista indisponivel.');const devices=(await r.json()).devices||[];list.innerHTML=devices.length?devices.map(d=>'<article class="entry"><i class="dot '+(d.revokedAt===null?'':'up')+'"></i><div><b>'+esc(d.label)+'</b><small>Evento '+esc(short(d.eventId))+' | visto '+fullTime(d.lastSeenAt)+'</small></div>'+(d.revokedAt===null?'<button class="device-action" data-revoke="'+esc(d.deviceId)+'">Bloquear</button>':'<time>bloqueado</time>')+'</article>').join(''):empty('Nenhum celular vinculado.');list.querySelectorAll('[data-revoke]').forEach(b=>b.onclick=async()=>{b.disabled=true;await fetch('/v1/cashier/revoke',{method:'POST',headers:{'Content-Type':'application/json','X-GTRZ-Key':key},body:JSON.stringify({deviceId:b.dataset.revoke})});loadCashiers()})}catch(e){list.innerHTML=empty(e.message||'Falha ao consultar celulares.')}}async function loadArchive(){const list=$('r2-list');try{const r=await fetch('/v1/monitor/archive',{headers:{'X-GTRZ-Key':key}});if(!r.ok)throw new Error('Arquivo indisponivel.');const objects=(await r.json()).objects||[];list.innerHTML=objects.length?objects.map(o=>entry('',o.key,String(o.size||0)+' bytes | '+String(o.etag||'').slice(0,12),o.uploadedAt)).join(''):empty('Nenhum objeto R2 arquivado ainda.')}catch(e){list.innerHTML=empty(e.message||'Falha ao consultar o arquivo.')}}async function refresh(){if(!key)return;try{const r=await fetch('/v1/monitor/snapshot',{headers:{'X-GTRZ-Key':key}});if(!r.ok)throw new Error(r.status===401?'Chave de pareamento invalida.':'A central nao respondeu.');render(await r.json());void loadCashiers();void loadArchive();$('notice').className='notice ok';$('notice').textContent='Central conectada. Atualizado as '+when(Date.now())+'.';$('dashboard').className=''}catch(e){$('notice').className='notice';$('notice').textContent=e.message||'Falha ao consultar a central.';$('dashboard').className='hidden'}}$('access').addEventListener('submit',e=>{e.preventDefault();key=$('key').value;refresh();if(timer)clearInterval(timer);timer=setInterval(refresh,3000)});
</script></body></html>`,
    { headers: { 'Content-Type': 'text/html; charset=UTF-8', 'Cache-Control': 'no-store' } },
  );
}
